import 'package:flutter/material.dart';

class RollingSwitch extends StatelessWidget {
  const RollingSwitch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Hello'),
      ),
    );
  }
}
